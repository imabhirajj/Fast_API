# Day 1 - SQLModel and SQLite Basics

## Topics Learned

- FastAPI setup
- SQLModel
- SQLite
- POST route
- GET route
- Session management

## Concepts Learned

- `table=True`
- `primary_key=True`
- `session.add()`
- `session.commit()`
- `session.refresh()`

## What I Built

- Created `notes.db` (configured as `note.db` in code)
- Stored notes permanently in SQLite
- Retrieved notes from database
